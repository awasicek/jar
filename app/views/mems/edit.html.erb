<h1>Edit your Memory.</h1>

<%= form_for @mem do |f| %>

    <div>
        <%= f.label :body %>
        <%= f.text_area :body %>
    </div>
    <div>
        <%= f.label :date %>
        <%= f.text_field :date %>
    </div>

    <%= f.submit "Submit these changes!" %>

<% end %>
